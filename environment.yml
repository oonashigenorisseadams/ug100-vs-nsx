name: ug100-vs-nsx
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - numpy
  - pyyaml
  - pysam>=0.21
  - samtools>=1.15
  - bcftools>=1.22
  - bedtools>=2.32
  # hap.py v0.3.15 is run via Singularity container
  # See README.md for container setup
